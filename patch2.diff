<<<<<<< SEARCH
    // Build dummy json body for create/update using fields that are createable/updateable
    let mut create_body = serde_json::Map::new();
    let mut update_body = serde_json::Map::new();

    for field in &describe.fields {
        if field.createable && field.name != "Id" {
            create_body.insert(
                field.name.clone(),
                json!(format!("{{{{${}}}}}", field.name)),
            );
        }
        if field.updateable && field.name != "Id" {
            update_body.insert(
                field.name.clone(),
                json!(format!("{{{{${}}}}}", field.name)),
            );
        }
    }
=======
    // Build dummy json body for create/update using fields that are createable/updateable
    let mut create_body = serde_json::Map::new();
    let mut update_body = serde_json::Map::new();

    for field in &describe.fields {
        if field.name != "Id" {
            let mut val = None;
            if field.createable {
                let v = json!(format!("{{{{${}}}}}", field.name));
                val = Some(v.clone());
                create_body.insert(field.name.clone(), v);
            }
            if field.updateable {
                let v = val.unwrap_or_else(|| json!(format!("{{{{${}}}}}", field.name)));
                update_body.insert(field.name.clone(), v);
            }
        }
    }
>>>>>>> REPLACE
