**Nova: HTML Form Generator**
**Learning:** Implementing HTML form generation from Salesforce metadata requires mapping SObject field types directly to HTML5 input types (e.g. email, tel, date).
**Action:** Remember to use the  tool to generate large files and always flatten string formatting when using .
**Nova: HTML Form Generator**
**Learning:** Implementing HTML form generation from Salesforce metadata requires mapping SObject field types directly to HTML5 input types (e.g. email, tel, date).
**Action:** Remember to use the write_file tool to generate large files and always flatten string formatting when using replace_with_git_merge_diff.
**Nova: HTML Form Generator**
**Learning:** Implementing HTML form generation from Salesforce metadata requires mapping SObject field types directly to HTML5 input types (e.g. email, tel, date).
**Action:** Remember to use the write_file tool to generate large files and always flatten string formatting when using replace_with_git_merge_diff.
