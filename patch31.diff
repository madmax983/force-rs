--- crates/force/src/api/files.rs
+++ crates/force/src/api/files.rs
@@ -120,10 +120,6 @@
         let status = response.status();
         let bytes = response.bytes().await.map_err(|e| ForceError::Http(crate::error::HttpError::RequestFailed(e)))?;

-        if !status.is_success() && status.as_u16() != 200 {
-            return Err(ForceError::InvalidInput("Failed to download ContentVersion".into()));
-        }
-
         Ok(bytes.to_vec())
     }
