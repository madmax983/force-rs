--- crates/force-sync/src/runtime.rs
+++ crates/force-sync/src/runtime.rs
@@ -341,20 +341,13 @@
                     )
                     .await;

-                match result {
-                    Ok(result) => {
-                        let link = project_sync_link(
-                            existing_link,
-                            envelope,
-                            result.salesforce_id.as_ref(),
-                            false,
-                        );
-                        self.store.put_link(&link).await?;
-                        self.store
-                            .ack_task_for_worker(&self.worker_id, task.task_id)
-                            .await?;
-                        Ok(true)
-                    }
-                    Err(error) => self.handle_apply_error(task, error).await,
-                }
+                let result = match result {
+                    Ok(res) => res,
+                    Err(error) => return self.handle_apply_error(task, error).await,
+                };
+                let link = project_sync_link(existing_link, envelope, result.salesforce_id.as_ref(), false);
+                self.store.put_link(&link).await?;
+                self.store
+                    .ack_task_for_worker(&self.worker_id, task.task_id)
+                    .await?;
+                Ok(true)
             }
             ChangeOperation::Delete => {
@@ -387,20 +380,12 @@
                     })?;

-                match self
+                if let Err(error) = self
                     .salesforce
                     .apply_rest_delete(envelope.sync_key().object_name(), &salesforce_id)
                     .await
                 {
-                    Ok(()) => {
-                        let link = project_sync_link(
-                            existing_link.into(),
-                            envelope,
-                            Some(&salesforce_id),
-                            true,
-                        );
-                        self.store.put_link(&link).await?;
-                        self.store
-                            .ack_task_for_worker(&self.worker_id, task.task_id)
-                            .await?;
-                        Ok(true)
-                    }
-                    Err(error) => self.handle_apply_error(task, error).await,
+                    return self.handle_apply_error(task, error).await;
                 }
+                let link = project_sync_link(existing_link.into(), envelope, Some(&salesforce_id), true);
+                self.store.put_link(&link).await?;
+                self.store
+                    .ack_task_for_worker(&self.worker_id, task.task_id)
+                    .await?;
+                Ok(true)
             }
         }
